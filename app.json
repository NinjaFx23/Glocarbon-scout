import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>🌍 GloCarbon</Text>
      <Text style={styles.subtitle}>Scout System Online</Text>
      <Text style={styles.status}>Status: Ready for Field Mapping</Text>
      <StatusBar style="light" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#121212', // Dark mode background
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    fontSize: 42,
    fontWeight: 'bold',
    color: '#00FFA3', // GC Neon Green
    marginBottom: 10,
  },
  subtitle: {
    fontSize: 20,
    color: '#FFFFFF',
    marginBottom: 30,
  },
  status: {
    fontSize: 16,
    color: '#A0A0A0',
    fontStyle: 'italic',
  }
});